apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: explainiq-orchestrator
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/maxScale: "10"
        autoscaling.knative.dev/minScale: "1"
        run.googleapis.com/cpu-throttling: "false"
        run.googleapis.com/execution-environment: gen2
    spec:
      containerConcurrency: 100
      timeoutSeconds: 300
      containers:
      - image: REGION-docker.pkg.dev/PROJECT_ID/REPO/explainiq-orchestrator:latest
        ports:
        - containerPort: 8080
        env:
        - name: GIN_MODE
          value: "release"
        - name: LOG_LEVEL
          value: "info"
        - name: AGENT_SUMMARIZER_URL
          value: "AGENT_SUMMARIZER_URL_PLACEHOLDER"
        - name: AGENT_EXPLAINER_URL
          value: "AGENT_EXPLAINER_URL_PLACEHOLDER"
        - name: AGENT_CRITIC_URL
          value: "AGENT_CRITIC_URL_PLACEHOLDER"
        - name: AGENT_VISUALIZER_URL
          value: "AGENT_VISUALIZER_URL_PLACEHOLDER"
        - name: GCP_PROJECT_ID
          value: "PROJECT_ID"
        - name: SERVICE_URL
          value: "SERVICE_URL_PLACEHOLDER"
        resources:
          limits:
            cpu: "2"
            memory: "2Gi"
          requests:
            cpu: "1"
            memory: "1Gi"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10

